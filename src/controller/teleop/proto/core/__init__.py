"""Compiled protobuf modules.

These files (``JointState_pb2.py``, ``RoveControl_pb2.py``) are generated
by running ``python build_protos.py`` from the project root. They are not
checked in because regenerating them is cheap and keeps the repo clean.
"""
